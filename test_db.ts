import { db, invoices } from './src/db'; import { eq } from 'drizzle-orm'; async function run() { const draft = await db.select().from(invoices).where(eq(invoices.codigoFactura, 'FAC-2026-000025')); const failed = await db.select().from(invoices).where(eq(invoices.codigoFactura, 'FAC-2026-000026')); console.log('Draft:', draft.length); console.log('Failed:', failed.length); if (draft.length) console.log(draft[0].status, draft[0].id); if (failed.length) console.log(failed[0].status, failed[0].id); process.exit(0); } run();
