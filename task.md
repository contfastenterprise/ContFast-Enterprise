# Lista de Tareas - Consolidación de Dependencias (Tarea 4.1)

- [x] Modificar `src/app/api/v1/customers/route.ts` para usar `getCache`/`setCache` en `GET` y `clearCachePattern` en `POST`.
- [x] Modificar `src/app/api/v1/customers/[id]/route.ts` para usar `getCache`/`setCache` en `GET` y `clearCachePattern` en `PUT` y `DELETE`.

## 🧪 2. Pruebas y Verificación
- [/] Validar tipación (`npx tsc --noEmit`).
- [ ] Validar compilación final (`npx next build`).
